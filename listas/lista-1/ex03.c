#include<stdio.h>
// calculo do numero ao cubo somento por multiplicação

int main() {
    int num;
    printf("Insira um valor: ");
    scanf("%d", &num);
    printf("O valor inserido ao cubo e %d", num * num * num);

    return 0;
}